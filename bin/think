#!/usr/bin/env python

import sys

from sense.client import Client

client = Client()

command = sys.argv[1]

if command == 'remember':
    print(client.remember(sys.argv[2], sys.argv[3]))
if command == 'remind':
    print(client.remind(sys.argv[2]))

